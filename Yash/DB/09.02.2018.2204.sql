--CREATE TABLE

--This statement is used to create table. Same for most of the query languages

CREATE TABLE Persons (
    PersonID int,
    LastName varchar(255),
    FirstName varchar(255),
    Address varchar(255),
    City varchar(255) 
);

--This makes the table with the name Persons. And the LastName and Adress etc. as the title for the column title.
